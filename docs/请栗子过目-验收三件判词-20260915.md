# 验收三件判词 · 请栗子过目（2026-09-15）

> 上一会话挂账的 E3：**D2 验收三件判词**（v6 17 卡 / 千人千面两拍板 / 标签分区）一直没带回栗子过目。
> 本轮补齐——每条都给「我判的结论 + 依据 + 需要你点头的那一句」，你只需回「行 / 改哪条」。

---

## 判词一：v6 试产 17 张卡

**我判：通过。**

依据：
- 六条批评全部修根后重跑（去反直觉角度词 / G4 禁代码块 / 单卡重评不取残留 / 装配去兜底截取 /
  装配全闸复检 / 覆盖度自由段数），qwen3.7-flash **6/6 真过**、qwen-flash 5/6（Deep-Live-Cam 被拒不再虚报）；
- 开头「这玩意」起手在 v6 复核里是 **0/6**（v5 是 8-12/12 的病灶）；
- 站点已清场重建 17 张全合格卡。

需你点头的一句话：**「17 张的文案质量可以上线」**——若你觉得还有哪张不行，报卡名，我单卡重评。

---

## 判词二：千人千面两拍板

**我判：机制成立，但**（本轮）**未全量落地。**

依据：
- 机制四件已定稿并有实证：提取前置（3-5 条独有事实清单）/ 主线强制（选 1 个语义重心）/
  具体性闸 / 双层可读；
- G-source 溯源硬闸（claim 独立断言 + source 原样引用 + claim≠source）已进生产 prompt 与校验；
- **但**：LLM 侧的千人千面段「全量跑」还没做（E5），目前是 prompt 已具备、数据未全量重生成。

需你点头的一句话：**「千人千面段是否与 E8 全文案重跑合并做」**——
合并做的话就是一次长跑窗口（时间账见验收报告 §六附录：6-10 小时），分开做则是两次。

---

## 判词三：标签分区（本轮已被体系重构覆盖）

**我判：v2.2 的标签分区判据有机制缺陷，本轮已按你的裁决重做（v3）。**

依据：
- v2.2 四区里 3 个是「交互方式」+ 1 个是「题材」，数学上不可能互斥 → 工具区兜底 48.7%，
  其中 33.3% 描述带 AI 关键词；
- v3 改成**三分支二分链**（作品 / 读物 / 智能本体 → 三问全否=工具），互斥由构造保证；
- skill 类 = 拿掉 AI 助手什么都不剩 → 问 3 → **AI**（你点名的「skill 出现在资源区」由此闭死）。

需你点头的一句话：**「二分链的顺序（作品 → 读物 → 智能本体）认可」**——
顺序决定了边界案例的归属（例：AI 绘画工作台按现在的顺序判「创意」而不是「AI」）。

---

## 附：三件之外、本轮新产生、需要你一句话的

| # | 事项 | 我的建议 | 你只需回 |
|---|---|---|---|
| 1 | 乐趣锚点集（`data/fun-anchors.json`，76 张） | 直接改文件即可，不用改代码 | 「锚点行」/ 或指出要删改哪几条 |
| 2 | C6 阈值线（重叠率 <35% / 高乐趣 ≥0.5 / 相似度 0.35） | 按现在的数执行 | 「阈值行」/ 或给新值 |
| 3 | 存量卡全文案重跑（E8） | 单独排窗口（6-10 小时长跑） | 「排窗口」/ 「先不做」 |
| 4 | 通道 `mistral` key 失效（401） | 机器已摘除；要恢复得换 key | 「换 key」/ 「不管它」 |
| 5 | 千人千面 LLM 段全量（E5） | 与 E8 合并做 | 「合并」/ 「分开」 |
