# GitTok 推荐池深度见底模拟与回填提案（H-07 / 0919-03 块一 B-07）

生成：2026-09-19 18:0x（本地）｜生成者：Qoder 会话 90fb6f1b｜**只出提案，不落地**（不改生产排序/推荐逻辑）
一句话结论：**按栗子口径的 100 张/人日，888 张合格池在 7 天滚动窗口下不见底（稳态仍有 248 张未看）；
只有重度到 200 张/人日才会第 6 天见底（100% 重复）。风险成立但门槛比预想高，提案按「先加可见度、暂不改排序」排。**

---

## 一、模型与口径（全部取自现物，非文档转述）

| 输入 | 取值 | 来源（可复跑） |
|---|---|---|
| 合格池 P | **888 张**（线上 2724 张里 `copyOk !== false`） | `curl -s https://chestnuts-sisyphus.github.io/gittok/data/feed.json` |
| 合格真率 | 32.6% | 同上 |
| 沉底规则 | 曝光即记 seen，降权 **0.45（<1天）/0.65（<3天）/0.85（<7天）/1.0（≥7天）** —— **不排除、只降权，满 7 天回权** | `web/src/App.tsx:530-538` |
| 等价结论 | 池子等效于 **7 天滚动窗口**（消耗上限 = D×7） | 推论 |
| 入池速率 | 新增卡 **26.5 张/日**，按合格率折算 **入池 8.6 张/日** | `data/feed.prev.json`(09-17 03:14) 与 `data/feed.json` 现物 repo 差集 ÷ 2.60 天 |
| 日均刷卡数 D | **无埋点** → 取 50/100/200 三档做敏感性（假设项） | 任务书口径估 100 张/人日 |

复跑：`node D:/tmp/h07_pool_depth.mjs`（读数 `D:/tmp/h07_pool_depth.json`）

## 二、结果表（第 N 天「未在本 7 天窗口内看过」的可用供给）

| D 张/日 | 第 1 天 | 第 5 天 | 第 8 天起（稳态） | 见底日 |
|---|---|---|---|---|
| 50 | 888 | 723 | **598** | 60 天内不见底 |
| 100 | 888 | 523 | **248** | 60 天内不见底 |
| 200 | 888 | 123 | **0（第 6 天起 100% 重复）** | **第 6 天** |

判读：
- **100 张/人日不成立见底**，但稳态只剩 248 张未看（占池 28%）——「今天刷到的有七成是这周看过的」这种感觉会真实出现，
  只是不出「无卡可推」的硬见底。
- 真正的约束不是池子总量，而是 **入池速率 8.6 张/日**：它远小于任何合理 D，意味着**池子只会被用旧、不会被补足**。
  当前 888 张是「一次性存量」（COPY-08 上线时打标出来的），G-17 每天只把 3~6 张洗进池。
- 37 天长尾跑完（1803 张不合格全部洗完）后池子会到 ≈2690 张，那时 D=200 也稳态充足 → **风险窗口是「长尾跑完之前」这 37~40 天**。

## 三、提案（等裁，全部未落地）

| # | 提案 | 代价 | 副作用 | Qoder 建议 |
|---|---|---|---|---|
| P-1 | **给推荐池加一条可见度**：在 `gittok-copy-audit.ts` 的「合格池规模环比」行之外，报表再打一行「按当前 8.6 张/日入池速率，D=100 稳态余量 248 张」 | 纯报表，已具备数据源（H-09 已加环比行） | 无 | **建议做**（一条 print） |
| P-2 | **降权回流**：把 `seenPenaltyOf` 的 7 天回权改成 3 天回权（让老卡更快回来） | 改生产排序语义（禁区：不改全站判定/排序口径） | 重复更早出现，观感回退 | 不做，等裁 |
| P-3 | **池内标记「你看过的旧版说明」**：前端对 seen 卡加一行「上周见过」的角标，把重复变成可解释 | 前端改动 + 一条审美断言 | 触碰「六项审美断言不许漂移」需复核 | 若栗子在意重复感，选这个而不是 P-2 |
| P-4 | **加速洗池**：G-17 提速四选项（prompt 自检清单／加 MAX_RETRY／换强通道／松闸口） | 前三项动 `VERSION` 或额度，第四项违反「全套文案」铁律 | 已在 0919-01 册等裁 | 与 B-03 并裁，不单开 |
| P-5 | **优先洗「高热度频道」的卡**：让洗池队列按频道需求排序，先补热门/分区·AI 那两个 ≥300 的硬频道 | 要改 recopy 取队列口径 | 与 V-C 频道容量口径耦合 | 需与 V-C 等裁项一起裁 |

## 四、本册未做的事（如实）

- 无真实曝光埋点：D 是假设档位，不是实测；上线埋点前「见底」只能按档位给区间。
- 未模拟多用户交叉（seen 是本机 localStorage，用户之间不共享，所以「人均见底」与「全站见底」不是一回事）。
- 未改任何生产排序/推荐代码（禁区）。
