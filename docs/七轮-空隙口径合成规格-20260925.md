# GitTok 空隙口径合成规格（七轮 · 2026-09-25）

> **状态行更新（八轮，2026-09-25 晚）**：**栗子在同一晚给出最终口径**，本规格的第一节规则被**取代**：
> 「首先不允许出现留白，我可以接受两列的时候卡片被缩短，但是第一张图片的那个卡片长度就是卡片极限长度了，
> 再长就要变成两列。」⇒ 见文末 **八轮追加**：**零留白** ＋ 卡宽 ≤793 ＋ **档内自适应**（摘要 1–2 行、
> 理由 3–7 行、卡高随行数长、标签槽位随卡宽收）。七轮那些「留白消不掉」的推导**只对七轮的规则集成立**，
> 八轮换了第三条口径（内容契约改由「加行」而不是「截断」满足）之后不再成立 —— 原文保留作决策轨迹。

> **本文件是仓内副本**（代码注释 `web/src/feed-layout.ts` / `web/src/styles.css` 指向的就是它）。
> 本机另存有一份同名阅读版（内容同源，仅头部说明不同）；**两份都改了才算改完**。
> **状态行**：✅ **已落码待验收**（规格 + 实现 + 闸三处同真）。上线 `dc4049b`（父 `9942a57`；CI 三 job 全绿、Deploy success）。
> 一句话结论：**中间带留白是几何必然，不是实现 bug**——三条历史口径里必须放弃一条；
> 本轮选择「保内容契约 + 保 793 上限」，同时把**理由被截**（同一条线上真截了 72/837 张）修掉。
> 证据：`D:/tmp/gt-layout/r7/`（gap-sweep / scan-* / overflow-* / gate 读数）。

---

## 一、规则（一条，可直接复算）

```
列数 n = max{ n : (W − (n−1)·16) / n ≥ 653 }        # W = 内容网格宽；653 = 一行放得下 35 字
卡宽   = n === 1 ? min(W, 793) : (W − (n−1)·16) / n  # 与 feed-layout.ts feedCardWidthFor() 同式
理由行数 = clamp(ceil(150 / floor((卡宽 − 69) / 13.94)), 3, 4)   # 见第三节
轨道留白 = W − 卡宽（单列档 = W − 793，**对称**分在两侧：justify-content: center）
```

- **轨道里不留空**（卡宽 = 轨道宽）——09-24 栗子红框那条继续成立；
- **轨道之外**的余量 = 对称页边距，**不左对齐、不单侧留**（左对齐是栗子 09-24 明确否掉的形态）；
- 三处同真：`web/src/feed-layout.ts`（推导）·`web/src/styles.css`（`--feed-card-max` /
  `--feed-reason-lines`）·`scripts/gittok-responsive-check.mjs`（R1/R4/R5 判据），由 lock 单测钉住。

## 二、为什么中间带一定有留白（三条口径互斥的证明）

设内容网格宽 W。历史三条口径：① 卡宽 ≤ 793（09-25 栗子）；② 单列不许大块空档（09-24 栗子）；
③ 内容契约不得截断（摘要 20–35 字一行、理由 100–150 字；〇块第 7 条「内容契约优先于版式」）。

- 由 ③ 反推卡宽的**允许区间**：摘要一行放 35 字 ⇒ 卡宽 ≥ 653；理由 3 行放 150 字 ⇒ 卡宽 ≥ 766。
  两列档每列卡宽 = (W−16)/2，单列档卡宽 = min(W, 793)。
- **W ∈ (793, 1322)**（＝视口 ≈1065–1594，覆盖 1366/1440/1500/1536 这些常见窗口）时：
  - 走 1 列 ⇒ 卡宽 = 793 ⇒ 余量 W−793 = 0…528px，**必然**成为留白（② 破）；
  - 走 2 列 ⇒ 卡宽 = (W−16)/2 = 389…653 < 653 ⇒ **摘要与理由都被截**（③ 破，实测见表）；
  - 取消上限 ⇒ 卡宽 = W = 794…1321 ⇒ 栗子 09-25 明确否掉的「太长了」（① 破，1600px 那张卡的原话）。
  ⇒ **三条不可同时成立**；本轮取 ①+③（内容优先），放弃 ② 的「中间带零留白」。

### 实测（全库 837 张，扫整条信息流；`D:/tmp/gt-layout/r7/scan-*.json`）

| 视口 | 网格 W | 列数 | 卡宽 | 两侧留白 | 摘要被截 | 理由被截（改前 3 行） |
|---|---:|---:|---:|---:|---:|---:|
| 1000×800 | 728 | 1 | 728 | 0 | 0/837 | 0/837 |
| 1100×800 | 828 | 1 | 793 | 35（18/侧） | 0/837 | 0/837 |
| 1200×900 | 928 | 1 | 793 | 135 | 0/837 | 0/837 |
| 1400×900 | 1128 | 1 | 793 | **335** | 0/837 | 0/837 |
| 1500×900 | 1228 | 1 | 793 | **435（218/侧）** | 0/837 | 0/837 |
| 1560×900 | 1288 | 1 | 793 | **495（248/侧）** | 0/837 | 0/837 |
| 1600×900 | 1328 | 2 | 656 | 0 | 0/837 | **72/837（8.6%）** |
| 1700×900 | 1428 | 2 | 706 | 0 | 0/837 | **7/837（0.8%）** |
| 1920×1080 | 1602 | 2 | 793 | 0 | 0/837 | 0/837 |

**若在中间带提前升 2 列**（任务书原推荐方案）——实测代价（`scan-inj2col1500.json` 等）：

| 卡宽 | 摘要被截（1 行） | 理由需要几行 | 理由被截（3 行） |
|---:|---:|---:|---:|
| 606（1500 档升 2 列） | **39/836（4.7%）** | 4 | **324/836（38.8%）** |
| 523（1920 档 3 列） | **276/837（33%）** | 5 | 731/837（87%） |
| 432（1600 档 3 列） | **668/837（80%）** | 6 | 833/837（99.5%） |

⇒ **「提前升 2 列消灭空隙」实测不可接受**：它把「空隙」换成了「省略号」，而省略号正是栗子同期
另一条反馈（`freestylefly/awesome-gpt-image-2` 那批）。这正是〇块第 11 条要防的「拆东墙补西墙」。

## 三、同轮修掉的那个**真**缺陷：理由行数写死 3 行

- 理由显示侧固定 `-webkit-line-clamp: 3`，而 3 行容量 = 3 × floor((卡宽−69)/13.94)
  **只对卡宽 ≥766 成立** ⇒ 1600 档 72/837（8.6%）、1700 档 7/837 的理由**线上真的带省略号**。
- 改法：行数由内容契约反推（同一条 150 字硬闸），下限 3、上限 4（4 行吃掉卡底 27px 余量后
  实测「可见撑破 0/837」；5 行会溢出 288 卡壳）。**卡高不用改**。
- 结果：1600/1700 档理由被截 **0/837**；793 档仍是 3 行（不动宽卡）；手机档保持 3 行（见下）。
- 闸已补 R5（该档行数 = 反推值 且 0 被截），并证明**植入即红**：把行数钉回 3 行 → R5 红。

## 四、被否决的三案（各写清理由，别再试）

| 方案 | 为什么否 |
|---|---|
| 只把 793 **居中改成左对齐** | 栗子 09-24 明确否过（700 左对齐留 296px 右空档「这种空隙不允许出现」）；今天换成 793 会留 435px 单侧空档，更差。闸 R4 已加「左右留量对称」判据，植入即红。 |
| 只**取消 793**（卡宽跟网格） | 回到栗子 09-25 否掉的「这个太长了」（1600 档实测 1228）；且跨过 1322 时卡宽 1321→653 断崖式跳变。闸 R4 + 卡宽预期表 + 对齐三条同时红。 |
| 只**改注释**说空隙合理 | 〇块第 12 条：注释与实现必须同真；改注释不改变像素，属自欺。本轮注释是**跟着实现**改的（并且删掉了与实现打架的旧段）。 |
| 提前升 2 列（任务书原推荐） | 上面「实测」表已量化：它是把「空隙」换成「省略号」。**闸里由 R1 守着**——R1「列数 ≥2 ⇒ 卡宽 ≥653」同时就是「禁止为消留白下调门槛」的锁（七轮在闸注释里写明这第二重身份）。 |

## 四·bis、三处相互咬合的判据（谁破哪条口径会红在哪）

| 口径 | 判据 | 位置 |
|---|---|---|
| 卡宽 ≤793 且单列档轨道有余量时**对称** | **R4**（七轮加严：= min(网格,793) 且 \|左−右\| ≤2px） | responsive 闸 |
| ≥2 列必须 ≥653（⇒ 禁止提前升列） | **R1**（四轮立、七轮补注第二重身份） | 同上 |
| 内容契约 0 截断（理由行数按卡宽反推） | **R5**（七轮新增）+ 锁单测 3 条 | 同上 + vitest |
| 逐档留白有多少（不做判据，只记数） | `READINGS[key].deadZone` + R4 详情 | 同上（读数 JSON） |

**植入即红自证**（四份注入 CSS：`D:/tmp/gt-layout/r7/red/`）：
`red-1-reason-3lines` → R5 红｜`red-2-leftalign` → R4 红（留量 0/335 不对称）＋对齐条红｜
`red-3-nocap` → R4＋卡宽预期表＋对齐＋轨道生效性 **4 条同时红**｜
**`red-4-promote2col`（＝本节表格第一行那个被否的方案）→ R1 红**：
@1600 是空转（该档本身就是 2 列 656，注入无效果），**@1400 命中**——
列数 2、卡宽 556 < 653（余量 **−97px**），联动 5 条红（R1＋卡宽预期表＋列数预期表＋多列档头铺满＋data-cols 生效性）。
⇒ 「升列维」不只是测量数据，闸注入已自证；R5 在该注入下记 SKIP（556 宽 4 行只装 102 字 < 150，
属「装不下」域）但照实打印「12 张里被截 10 张」。

## 五、待他裁的最小问题（三选一，各有代价）

**问题：中间带（视口 ≈1065–1594、两侧留白 0–248px）要不要为了让卡片铺满而破一条口径？**

| 选项 | 空隙 | 卡宽 | 内容契约 | 代价 |
|---|---|---|---|---|
| **A 维持本轮（推荐）** | 留白 ≤248/侧 | ≤793 ✓ | 0 截 ✓ | 中间带有对称页边距（就是您截图红框那两处） |
| B 中间带提前升 2 列 | 消失 | 389–653（更窄） | **摘要 4.7–80%、理由 38.8–99.5% 被截** | 与「两列及以上必须全展示」直接冲突；要么改契约、要么给窄卡加行（卡高 288→≈400，卡片形态变样） |
| C 中间带取消 793（卡宽跟网格） | 消失 | 794–1321（超 793 上限 1.67×） | 0 截 ✓ | 回到「太长了」；需同时把 2 列门槛挪到 1520，否则跨 1322 时卡宽 1321→653 断崖 |

**我（ZCode）的建议**：维持 A。理由：B 会把留白换成省略号（您同期另一条反馈），C 推翻您
09-25 刚定的 793 上限；A 是唯一「不动任何已定口径」的解。若您确实更在意留白，**C 是代价最小
的破法**（不截断、不改卡片形态），只需您一句话把单列上限从 793 抬到某个数（或改为「跟网格」）。

---

## 附：本轮同源的两条登记（不属本规格，但同一次会话产出）

1. **理由省略号线上+本地反查**：`D:/tmp/gt-layout/r7/G4-理由截断反查.md`
   ——线上 ≡ 本地（资产同名同哈希）；1500 档（栗子截图档）摘要/理由 **0/837** 被截；
   真源头＝上面第三节的固定 3 行 ＋ **数据本身断句 573/2958（19.4%）**（看着像截断，实际没截）。
2. **Mimosa 记账**：`D:/tmp/gt-layout/r7/G6-Mimosa-记账.md`
   ——钩子在**仓外**（`.husky/pre-commit` 不调 Mimosa、仓内无 Mimosa 源码）；`scanner_enobufs` 不修；
   **不宣称项目安全**。

---

# 八轮追加（2026-09-25 晚）—— 栗子给定版口径（本规格的最终规则）

## 一、栗子的两条指令（原话）

1. 「**首先不允许出现留白**，我可以接受两列的时候卡片被缩短，但是第一张图片的那个卡片长度就是卡片极限
   长度了，**再长就要变成两列**。」
2. 「第二张图片所示，**完全错位了，所有的排版出现了问题**，一点都不够优雅流畅统一，观感很难受。」

⇒ ① 废弃七轮「中间带留白消不掉」的结论（那是保内容契约的**代价**，不是必须）；② 列数改由**上限**反解；
③ 新增一条硬要求：**档内统一**（同一档里所有卡的行数/高度/标签行必须一致，不许参差）。

## 二、八轮规则（取代第一节）

```
列数 n = 最小 n 使 (W − (n−1)×16) / n ≤ 793        # 超过 793 就加列 ⇒ 轨道铺满、零留白
卡宽   = n === 1 ? min(W, 793) : (W − (n−1)×16) / n
摘要行数 S = 一行放得下 35 字（floor((卡宽−69)/16.66) ≥ 35）? 1 : 2
理由行数 R = clamp(ceil(150 / floor((卡宽−69)/13.94)), 3, 7)
卡高   H = 288 + (S−1)×25 + (R−3)×23.7              # 卡底余量恒 27px（跨档同节奏）
标签槽位 T = clamp(floor((卡宽−69)/88), 2, 8)        # 余量折进 +N；永不换行 ⇒ 无半截 chip
```

**为什么「窄卡」不用截断兜**：七轮的实测已证 3 行只对卡宽 ≥766 成立（1600 档 72/837 被截）。
八轮把「卡变窄」的代价从「截断」改成「**加行 + 加高**」，内容契约因此在全档成立；
新增的代价是**卡高随档变化**（288→408），由 `--feed-card-h` 逐档下发，虚拟列表垫片同值。

## 三、逐档读数（本机 dist 实测，responsive 闸 R0/R7 读数逐档可复算）

| 视口 | 网格 | 列数 | 卡宽 | S | R | H | T | 留白 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 1920/2560 | 1602 | 2 | 793 | 1 | 3 | 288 | 8 | **0** |
| 1600 | 1328 | 2 | 656 | 1 | 4 | 312 | 6 | **0** |
| 1500 | 1228 | 2 | 606 | 2 | 4 | 337 | 6 | **0** |
| 1400 | 1128 | 2 | 556 | 2 | 5 | 360 | 5 | **0** |
| 1275 | 1003 | 2 | 494 | 2 | 5 | 360 | 4 | **0** |
| 1200 | 928 | 2 | 456 | 2 | 6 | 384 | 4 | **0** |
| 1100 | 828 | 2 | 406 | 2 | 7 | 408 | 3 | **0** |
| 1000 | 728 | 1 | 728 | 1 | 4 | 312 | 7 | **0** |
| 900 | 628 | 1 | 628 | 2 | 4 | 337 | 5 | **0** |
| 844×390（窄高） | 572 | 1 | 572 | 1 | 2 | 210 | 5 | **0** |
| ≤768 手机 | vw−48 | 1 | vw−48 | 1 | 3 | 288/210 | 2–3 | **0** |

全库 837 张扫整条信息流：**摘要被截 0/837、理由被截 0/837、标签行数全 1 行**（1100/1400/1500 三档各扫一遍）。

## 四、闸（八轮新增/改判）

| 判据 | 内容 | 植入即红 |
|---|---|---|
| **R0（新）** | **零留白**：n×卡宽 + (n−1)×行距 == 网格宽（±1.5px） | 注入七轮那种「1 列 793 + 居中」→ 7 条红（含 R0/R1） |
| **R1（改）** | 列数 == 上限反解值（最小 n 使卡宽 ≤793） | 同上 |
| **R4（改）** | 卡宽 ≤793 全档（上限升格为列数依据） | 注入「取消上限」→ 4 条红 |
| **R5（扩）** | 理由行数 == 算式值 且 0 被截（3–7 行） | 注入「钉回 3 行」→ R5 红 |
| **R6（新）** | 摘要 0 被截（2 行槽位靠换行而不是省略号） | 注入「摘要压回 1 行」→ R6 + H-01 红 |
| **R7（新）** | 档内形态：卡高 == 算式值、标签不换行（无半截 chip） | 同上 |

## 五、本轮同时修掉的三类「不统一」

1. **卡底余量不一致**：七轮 4 行档把 27px 余量吃到 3px ⇒ 八轮改成「加行就加高」，余量恒 27px；
2. **标签半截 chip**：`.card-tags` 26px 定高 + `gap:4px` 时换行的第二行会露出 2px 残片
   ⇒ 槽位估算（不换行）+ 行距 8px（换了也不露）双保险；
3. **`-webkit-line-clamp` 行数与卡高脱钩**：旧版靠 clamp 硬裁，新版 clamp 与 `--feed-card-h` 同源。

## 六、遗留 / 待他裁

1. **2 行摘要槽位里，短摘要会留一条空白**（约 76% 的卡在 606 档只有 1 行文字、槽位是 2 行）。
   备选：窄档摘要用**流式字号**（0.98→0.82rem）把 1 行容量顶到 35 字 ⇒ 1 行档位下移到卡宽 ≈557；
   代价是档间字号不一致。**本轮取「槽位固定」（对齐优先），留给他裁。**
2. 手机档（≤768）仍是「按设计收窄」（摘要 18–19 字、理由 3 行、会截断）——六轮 G9① 的待裁项，本轮未动。
3. 数据侧断句 573/2958（19.4%）仍归 E-8 重跑。
